# Environments

Docker-compose files for local environments go here, one per
"host system + Document Server" pair:

- `docker-compose.alfresco.yml` — Alfresco + onlyoffice-alfresco plugin + Document Server
